[project]
name = "production-ml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rutul D. Bhosale", email = "bhosalerutul117@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "psycopg[binary]>=3.3.6",
    "pymongo>=4.18.2",
    "sqlalchemy>=2.1.0",
    "uvicorn>=0.54.0",
]

[project.scripts]
production-ml = "production_ml:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
